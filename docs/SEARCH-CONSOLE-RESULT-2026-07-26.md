# Search Console — risultato 26 luglio 2026

## Proprietà

```text
sc-domain:senzaroaming.it
```

Il service account `senza-roaming@soliwkr.iam.gserviceaccount.com` vede la proprietà con livello `siteOwner`.

## Sitemap

Submission manuale completata:

```text
https://senzaroaming.it/sitemap.xml
```

Subito dopo l’invio Search Console mostrava `0 pagine rilevate`. Questo valore iniziale non viene interpretato come errore e non autorizza submission ripetute.

## Indicizzazione

Non è stata usata la Google Indexing API. Le pagine pubbliche correnti non vengono forzate singolarmente perché homepage e listing devono ancora essere riallineate a una keyword map e a intenti SEO definitivi.

Il prossimo checkpoint SEO riguarda:

```text
keyword map
→ architettura contenuti
→ copy homepage e listing
→ prime pagine forti
→ eventuale richiesta manuale delle sole URL prioritarie
```

Preview, Control Room, API, redirect, pagine `review`, 404 e file tecnici restano esclusi dall’indicizzazione.
